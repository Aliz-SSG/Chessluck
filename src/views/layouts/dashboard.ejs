<!DOCTYPE html>
<%- include ('../partials/messages.ejs') -%>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Dashboard</title>
        <link rel="icon" type="image/x-icon" href="/images/faveicon.ico">
        <script src="https://cdn.tailwindcss.com"></script>
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
        <style>
            .chess-board {
                background-image: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="100" height="100"><rect width="50" height="50" x="0" y="0" fill="%23374151"/><rect width="50" height="50" x="50" y="50" fill="%23374151"/><rect width="50" height="50" x="0" y="50" fill="%23212a3e"/><rect width="50" height="50" x="50" y="0" fill="%23212a3e"/></svg>');
                background-size: 100px 100px;
            }

            .gradient-text {
                background: linear-gradient(90deg, #6366f1, #8b5cf6);
                -webkit-background-clip: text;
                background-clip: text;
                color: transparent;
            }

            .pulse-animation {
                animation: pulse 2s infinite;
            }

            @keyframes pulse {
                0% {
                    box-shadow: 0 0 0 0 rgba(99, 102, 241, 0.7);
                }

                70% {
                    box-shadow: 0 0 0 10px rgba(99, 102, 241, 0);
                }

                100% {
                    box-shadow: 0 0 0 0 rgba(99, 102, 241, 0);
                }
            }

            .move-history::-webkit-scrollbar {
                width: 5px;
            }

            .move-history::-webkit-scrollbar-track {
                background: #1e293b;
            }

            .move-history::-webkit-scrollbar-thumb {
                background: #4b5563;
                border-radius: 10px;
            }

            .sticky-profile {
                position: sticky;
                top: 20px;
                z-index: 10;
            }

            @media (max-width: 1023px) {
                .sticky-profile {
                    position: static;
                }
            }

            /* Custom scrollbar */
            .custom-scrollbar::-webkit-scrollbar {
                width: 6px;
            }

            .custom-scrollbar::-webkit-scrollbar-track {
                background: #1a202c;
            }

            .custom-scrollbar::-webkit-scrollbar-thumb {
                background: #4a5568;
                border-radius: 3px;
            }

            .custom-scrollbar::-webkit-scrollbar-thumb:hover {
                background: #718096;
            }

            /* Pulse animation for online status */
            @keyframes pulse {
                0% {
                    opacity: 1;
                }

                50% {
                    opacity: 0.5;
                }

                100% {
                    opacity: 1;
                }
            }

            .pulse {
                animation: pulse 2s infinite;
            }
        </style>
    </head>

    <body class="bg-gray-900 text-gray-200 min-h-screen">
        <nav class="bg-gray-800 border-b border-gray-700 py-4 px-6 sticky top-0 z-50">
            <div class="max-w-7xl mx-auto flex justify-between items-center">
                <div class="flex items-center space-x-2">
                    <i class="fas fa-chess-queen text-blue-400 text-2xl"></i>
                    <span
                        class="text-xl font-bold bg-gradient-to-r from-blue-400 to-purple-500 bg-clip-text text-transparent hover:text-blue-400 transition"><a
                            href="http://localhost:5000/">ChessMaster</a></span>
                </div>

                <div class="hidden md:flex space-x-6">
                   
                    <form action="/game/play/<%= user.id %>" method="post" class="hover:text-blue-400 transition">
                      <button>Play</button>
                    </form>
                    
                    <a href="http://localhost:5000/learn" class="hover:text-blue-400 transition">Puzzles</a>
                    <a href="http://localhost:5000/learn" class="hover:text-blue-400 transition">Learn</a>
                    <a href="http://localhost:5000/friends" class="hover:text-blue-400 transition">Friends</a>
                </div>

                <div class="flex items-center space-x-4">
                    <button class="bg-blue-600 hover:bg-blue-700 px-4 py-2 rounded-md transition"><a
                            href="/auth/changepassword">Change password</a></button>
                    <button class="hidden md:block bg-gray-700 hover:bg-gray-600 px-4 py-2 rounded-md transition"><a
                            href="/auth/logout">Logout</a></button>
                    <button class="md:hidden text-xl">
                        <i class="fas fa-bars"></i>
                    </button>
                </div>
            </div>
        </nav>

        <!-- Main Content -->
        <main class="flex-1 p-6 overflow-y-auto">
            <div class="flex justify-between items-center mb-6">
              <h2 class="text-2xl font-bold ">Welcome back <span class="bg-gradient-to-r from-blue-400 to-purple-500 bg-clip-text text-transparent"><%= user.username %></span></h2>
              <div class="flex space-x-3">
                <form action="/game/play/<%= user._id %>" method="post">
                  <button type="submit"
                    class="bg-indigo-600 hover:bg-indigo-700 px-4 py-2 rounded-lg flex items-center space-x-2 transition">
                    <i class="fas fa-plus"></i>
                    <span>New Game</span>
                  </button>
                <!-- </form>
                <a href="/user/profile"
                  class="bg-gray-700 hover:bg-gray-600 px-4 py-2 rounded-lg flex items-center space-x-2 transition">
                  <i class="fas fa-user"></i>
                  <span>Profile</span>
                </a> -->
              </div>
            </div>
          
            <!-- Stats Cards -->
            <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
              <div class="bg-gradient-to-r from-green-600 to-green-700 p-6 rounded-xl text-white">
                <div class="flex items-center justify-between">
                  <div>
                    <p class="text-green-100 text-sm">Wins</p>
                    <p class="text-3xl font-bold"><%= user.wins %></p>
                  </div>
                  <i class="fas fa-trophy text-3xl opacity-80"></i>
                </div>
              </div>
              
              <div class="bg-gradient-to-r from-red-600 to-red-700 p-6 rounded-xl text-white">
                <div class="flex items-center justify-between">
                  <div>
                    <p class="text-red-100 text-sm">Losses</p>
                    <p class="text-3xl font-bold"><%= user.losses %></p>
                  </div>
                  <i class="fas fa-times-circle text-3xl opacity-80"></i>
                </div>
              </div>
              
              <div class="bg-gradient-to-r from-yellow-600 to-yellow-700 p-6 rounded-xl text-white">
                <div class="flex items-center justify-between">
                  <div>
                    <p class="text-yellow-100 text-sm">Draws</p>
                    <p class="text-3xl font-bold"><%= user.draws %></p>
                  </div>
                  <i class="fas fa-handshake text-3xl opacity-80"></i>
                </div>
              </div>
              
              <div class="bg-gradient-to-r from-blue-600 to-blue-700 p-6 rounded-xl text-white">
                <div class="flex items-center justify-between">
                  <div>
                    <p class="text-blue-100 text-sm">Win Rate</p>
                    <p class="text-3xl font-bold"><%= user.winRate %>%</p>
                  </div>
                  <i class="fas fa-chart-line text-3xl opacity-80"></i>
                </div>
              </div>
            </div>
            <!DOCTYPE html>
            <html lang="en">
            <head>
              <meta charset="UTF-8">
              <meta name="viewport" content="width=device-width, initial-scale=1.0">
              <title>Animated Flex Cards (Tailwind)</title>
              <script src="https://cdn.tailwindcss.com"></script>
              <style>
                /* extra transitions that Tailwind doesn’t cover directly */
                input:checked + label {
                  width: 600px;
                }
                input:checked + label .description {
                  opacity: 1 !important;
                  transform: translateY(0) !important;
                }
                .description {
                  transition: all .3s ease;
                  transition-delay: .3s;
                }
              </style>
            </head>
            <div class="w-full h-[450px] flex items-center justify-center"><span class="card w-[400px] h-[400px] bg-gradient-to-r from-blue-800 to-blue-900 cursor-default overflow-hidden rounded-2xl flex items-center justify-center shadow-[0_10px_30px_-5px_rgba(0,0,0,0.8)] text-white text-[18px] text-center">
              ==>
              This Week's Available Decks
              ==>
            </span>
              <div class="h-[400px] flex flex-nowrap justify-start">
                <% 
                  const gradients = [
                    "from-red-500 to-yellow-500",
                    "from-blue-500 to-green-500",
                    "from-purple-500 to-pink-500",
                    "from-indigo-500 to-teal-500",
                    "from-orange-500 to-rose-500"
                  ];
                %>
                <% decks.forEach((deck, index) => { %>
                  <input type="radio" name="slide" id="c<%= index + 1 %>" class="hidden" <%= index === 0 ? "checked" : "" %>>
                  <label for="c<%= index + 1 %>" 
                         class="card relative w-20 mx-2 cursor-pointer overflow-hidden rounded-2xl flex items-end shadow-[0_10px_30px_-5px_rgba(0,0,0,0.8)] transition-[width] duration-[600ms] ease-[cubic-bezier(.28,-0.03,0,.99)]">
                    
                    <!-- Background image -->
                    <div class="absolute inset-0 bg-cover bg-center" style="background-image: url('<%= deck.image %>');"></div>
                    
                    <!-- Gradient overlay -->
                    <div class="absolute inset-0 bg-gradient-to-r <%= gradients[index % gradients.length] %> opacity-70"></div>
                    
                    <!-- Content -->
                    <div class="relative row flex text-white flex-nowrap">
                      <div class="icon bg-[#223] rounded-full w-12 h-12 m-4 flex justify-center items-center">
                        <%= index + 1 %>
                      </div>
                      <div class="description flex flex-col justify-center h-20 w-[520px] opacity-0 translate-y-8">
                        <h4 class="uppercase text-[24px] drop-shadow-[0_1.2px_1.2px_rgba(0,0,0,0.8)]
                        "><%= deck.name %></h4>
                        <p class="text-[gray-400] pt-1"><%= deck.description %></p>
                      </div>
                    </div>
                  </label>
                <% }) %>
              </div>
            </div>
            
            
            </html>
            
          
            
            <script>
                document.addEventListener('DOMContentLoaded', function () {
                    console.log('Chess dashboard loaded');
                    
                    // Add some interactive elements
                    const statsCards = document.querySelectorAll('.bg-gradient-to-r');
                    statsCards.forEach(card => {
                        card.addEventListener('mouseenter', function() {
                            this.style.transform = 'translateY(-2px)';
                            this.style.transition = 'transform 0.2s ease';
                        });
                        card.addEventListener('mouseleave', function() {
                            this.style.transform = 'translateY(0)';
                        });
                    });
                });
            </script>
    </body>

</html>